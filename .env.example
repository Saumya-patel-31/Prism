# Copy to .env.local and fill in.

# Signs session cookies. REQUIRED in production — the server refuses to
# start without it. Generate one with:  openssl rand -hex 32
AUTH_SECRET=

# Where Ollama is listening. Defaults to http://localhost:11434 if unset.
OLLAMA_HOST=http://localhost:11434
